# This file is kept for reference only.
# The NixOS modules have moved to nix/:
#
#   nix/module-vpn.nix  — WireGuard/systemd backend (vendored from tadfisher/flake)
#   nix/module-gui.nix  — GTK4/Rust GUI frontend
#
# The flake exports:
#   nixosModules.default  — combined (recommended for most users)
#   nixosModules.pia-vpn  — backend only
#   nixosModules.pia-gui  — frontend only
